apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-conductor-chart.fullname" . }}-test-connection"
  labels:
    {{- include "test-conductor-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-conductor-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
