apiVersion: v1
kind: Service
metadata:
  name: {{ include "qtest-launch.fullname" . }}
  labels:
    {{- include "qtest-launch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qtest-launch.selectorLabels" . | nindent 4 }}
